//Challenge Area

let person = {
    name : 'Peter',
    age : 25,
    location: 'San Diego'
    
}

console.log(`${person.name} is ${person.age} years and lives in ${person.location}`)

person.age = 30
console.log(`${person.name} is ${person.age} years and lives in ${person.location}`)
